
var Schema = require('../../../lib').Schema;
var mySchema = Schema({ name: String ,meta: {
    votes: Number,
    favs:  Number
  }
	});

// db is global
//var mongoose = require('../../../lib')
//module.exports = mongoose.model('MyModel', mySchema);
module.exports = db.model('MyModel', mySchema);
